package com.scalea.controllers;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;

/*
 * th:each - loop
 * th:if - condition
 * th:unless - condition
 * th:switch - switch case thing
 */
@Controller
public class HomeController {
	
}
